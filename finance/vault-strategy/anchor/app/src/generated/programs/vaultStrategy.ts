/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertIsInstructionWithAccounts,
  containsBytes,
  extendClient,
  fixEncoderSize,
  getBytesEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
  SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
  SolanaError,
  type Address,
  type ClientWithPayer,
  type ClientWithRpc,
  type ClientWithTransactionPlanning,
  type ClientWithTransactionSending,
  type ExtendedClient,
  type GetAccountInfoApi,
  type GetMultipleAccountsApi,
  type Instruction,
  type InstructionWithData,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  addSelfFetchFunctions,
  addSelfPlanAndSendFunctions,
  type SelfFetchFunctions,
  type SelfPlanAndSendFunctions,
} from "@solana/program-client-core";
import {
  getApprovedAssetCodec,
  getAssetConfigCodec,
  getRegistryCodec,
  getStrategyCodec,
  type ApprovedAsset,
  type ApprovedAssetArgs,
  type AssetConfig,
  type AssetConfigArgs,
  type Registry,
  type RegistryArgs,
  type Strategy,
  type StrategyArgs,
} from "../accounts";
import {
  getAddAssetInstructionAsync,
  getApproveAssetInstructionAsync,
  getCollectFeesInstructionAsync,
  getDepositInstructionAsync,
  getInitializeRegistryInstructionAsync,
  getInitializeStrategyInstructionAsync,
  getRebalanceInstructionAsync,
  getSetWeightInstruction,
  getWithdrawInstructionAsync,
  parseAddAssetInstruction,
  parseApproveAssetInstruction,
  parseCollectFeesInstruction,
  parseDepositInstruction,
  parseInitializeRegistryInstruction,
  parseInitializeStrategyInstruction,
  parseRebalanceInstruction,
  parseSetWeightInstruction,
  parseWithdrawInstruction,
  type AddAssetAsyncInput,
  type ApproveAssetAsyncInput,
  type CollectFeesAsyncInput,
  type DepositAsyncInput,
  type InitializeRegistryAsyncInput,
  type InitializeStrategyAsyncInput,
  type ParsedAddAssetInstruction,
  type ParsedApproveAssetInstruction,
  type ParsedCollectFeesInstruction,
  type ParsedDepositInstruction,
  type ParsedInitializeRegistryInstruction,
  type ParsedInitializeStrategyInstruction,
  type ParsedRebalanceInstruction,
  type ParsedSetWeightInstruction,
  type ParsedWithdrawInstruction,
  type RebalanceAsyncInput,
  type SetWeightInput,
  type WithdrawAsyncInput,
} from "../instructions";
import {
  findApprovedAssetPda,
  findRegistryPda,
  findShareMintPda,
  findStrategyPda,
} from "../pdas";

export const VAULT_STRATEGY_PROGRAM_ADDRESS =
  "VLT5W7bqhRN4nCdRpXm8UfHRxZd9EuZGqiSAkGHQfGh" as Address<"VLT5W7bqhRN4nCdRpXm8UfHRxZd9EuZGqiSAkGHQfGh">;

export enum VaultStrategyAccount {
  ApprovedAsset,
  AssetConfig,
  Registry,
  Strategy,
}

export function identifyVaultStrategyAccount(
  account: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): VaultStrategyAccount {
  const data = "data" in account ? account.data : account;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([187, 133, 252, 71, 239, 199, 204, 13]),
      ),
      0,
    )
  ) {
    return VaultStrategyAccount.ApprovedAsset;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([57, 112, 247, 166, 247, 64, 140, 23]),
      ),
      0,
    )
  ) {
    return VaultStrategyAccount.AssetConfig;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([47, 174, 110, 246, 184, 182, 252, 218]),
      ),
      0,
    )
  ) {
    return VaultStrategyAccount.Registry;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([174, 110, 39, 119, 82, 106, 169, 102]),
      ),
      0,
    )
  ) {
    return VaultStrategyAccount.Strategy;
  }
  throw new SolanaError(
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
    { accountData: data, programName: "vaultStrategy" },
  );
}

export enum VaultStrategyInstruction {
  AddAsset,
  ApproveAsset,
  CollectFees,
  Deposit,
  InitializeRegistry,
  InitializeStrategy,
  Rebalance,
  SetWeight,
  Withdraw,
}

export function identifyVaultStrategyInstruction(
  instruction: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): VaultStrategyInstruction {
  const data = "data" in instruction ? instruction.data : instruction;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([81, 53, 134, 142, 243, 73, 42, 179]),
      ),
      0,
    )
  ) {
    return VaultStrategyInstruction.AddAsset;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([127, 15, 21, 247, 23, 22, 189, 238]),
      ),
      0,
    )
  ) {
    return VaultStrategyInstruction.ApproveAsset;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([164, 152, 207, 99, 30, 186, 19, 182]),
      ),
      0,
    )
  ) {
    return VaultStrategyInstruction.CollectFees;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([242, 35, 198, 137, 82, 225, 242, 182]),
      ),
      0,
    )
  ) {
    return VaultStrategyInstruction.Deposit;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([189, 181, 20, 17, 174, 57, 249, 59]),
      ),
      0,
    )
  ) {
    return VaultStrategyInstruction.InitializeRegistry;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([208, 119, 144, 145, 178, 57, 105, 252]),
      ),
      0,
    )
  ) {
    return VaultStrategyInstruction.InitializeStrategy;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([108, 158, 77, 9, 210, 52, 88, 62]),
      ),
      0,
    )
  ) {
    return VaultStrategyInstruction.Rebalance;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([41, 228, 200, 244, 87, 125, 173, 36]),
      ),
      0,
    )
  ) {
    return VaultStrategyInstruction.SetWeight;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([183, 18, 70, 156, 148, 109, 161, 34]),
      ),
      0,
    )
  ) {
    return VaultStrategyInstruction.Withdraw;
  }
  throw new SolanaError(
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
    { instructionData: data, programName: "vaultStrategy" },
  );
}

export type ParsedVaultStrategyInstruction<
  TProgram extends string = "VLT5W7bqhRN4nCdRpXm8UfHRxZd9EuZGqiSAkGHQfGh",
> =
  | ({
      instructionType: VaultStrategyInstruction.AddAsset;
    } & ParsedAddAssetInstruction<TProgram>)
  | ({
      instructionType: VaultStrategyInstruction.ApproveAsset;
    } & ParsedApproveAssetInstruction<TProgram>)
  | ({
      instructionType: VaultStrategyInstruction.CollectFees;
    } & ParsedCollectFeesInstruction<TProgram>)
  | ({
      instructionType: VaultStrategyInstruction.Deposit;
    } & ParsedDepositInstruction<TProgram>)
  | ({
      instructionType: VaultStrategyInstruction.InitializeRegistry;
    } & ParsedInitializeRegistryInstruction<TProgram>)
  | ({
      instructionType: VaultStrategyInstruction.InitializeStrategy;
    } & ParsedInitializeStrategyInstruction<TProgram>)
  | ({
      instructionType: VaultStrategyInstruction.Rebalance;
    } & ParsedRebalanceInstruction<TProgram>)
  | ({
      instructionType: VaultStrategyInstruction.SetWeight;
    } & ParsedSetWeightInstruction<TProgram>)
  | ({
      instructionType: VaultStrategyInstruction.Withdraw;
    } & ParsedWithdrawInstruction<TProgram>);

export function parseVaultStrategyInstruction<TProgram extends string>(
  instruction: Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array>,
): ParsedVaultStrategyInstruction<TProgram> {
  const instructionType = identifyVaultStrategyInstruction(instruction);
  switch (instructionType) {
    case VaultStrategyInstruction.AddAsset: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultStrategyInstruction.AddAsset,
        ...parseAddAssetInstruction(instruction),
      };
    }
    case VaultStrategyInstruction.ApproveAsset: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultStrategyInstruction.ApproveAsset,
        ...parseApproveAssetInstruction(instruction),
      };
    }
    case VaultStrategyInstruction.CollectFees: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultStrategyInstruction.CollectFees,
        ...parseCollectFeesInstruction(instruction),
      };
    }
    case VaultStrategyInstruction.Deposit: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultStrategyInstruction.Deposit,
        ...parseDepositInstruction(instruction),
      };
    }
    case VaultStrategyInstruction.InitializeRegistry: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultStrategyInstruction.InitializeRegistry,
        ...parseInitializeRegistryInstruction(instruction),
      };
    }
    case VaultStrategyInstruction.InitializeStrategy: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultStrategyInstruction.InitializeStrategy,
        ...parseInitializeStrategyInstruction(instruction),
      };
    }
    case VaultStrategyInstruction.Rebalance: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultStrategyInstruction.Rebalance,
        ...parseRebalanceInstruction(instruction),
      };
    }
    case VaultStrategyInstruction.SetWeight: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultStrategyInstruction.SetWeight,
        ...parseSetWeightInstruction(instruction),
      };
    }
    case VaultStrategyInstruction.Withdraw: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultStrategyInstruction.Withdraw,
        ...parseWithdrawInstruction(instruction),
      };
    }
    default:
      throw new SolanaError(
        SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
        {
          instructionType: instructionType as string,
          programName: "vaultStrategy",
        },
      );
  }
}

export type VaultStrategyPlugin = {
  accounts: VaultStrategyPluginAccounts;
  instructions: VaultStrategyPluginInstructions;
  pdas: VaultStrategyPluginPdas;
  identifyAccount: typeof identifyVaultStrategyAccount;
  identifyInstruction: typeof identifyVaultStrategyInstruction;
  parseInstruction: typeof parseVaultStrategyInstruction;
};

export type VaultStrategyPluginAccounts = {
  approvedAsset: ReturnType<typeof getApprovedAssetCodec> &
    SelfFetchFunctions<ApprovedAssetArgs, ApprovedAsset>;
  assetConfig: ReturnType<typeof getAssetConfigCodec> &
    SelfFetchFunctions<AssetConfigArgs, AssetConfig>;
  registry: ReturnType<typeof getRegistryCodec> &
    SelfFetchFunctions<RegistryArgs, Registry>;
  strategy: ReturnType<typeof getStrategyCodec> &
    SelfFetchFunctions<StrategyArgs, Strategy>;
};

export type VaultStrategyPluginInstructions = {
  addAsset: (
    input: AddAssetAsyncInput,
  ) => ReturnType<typeof getAddAssetInstructionAsync> &
    SelfPlanAndSendFunctions;
  approveAsset: (
    input: ApproveAssetAsyncInput,
  ) => ReturnType<typeof getApproveAssetInstructionAsync> &
    SelfPlanAndSendFunctions;
  collectFees: (
    input: MakeOptional<CollectFeesAsyncInput, "payer">,
  ) => ReturnType<typeof getCollectFeesInstructionAsync> &
    SelfPlanAndSendFunctions;
  deposit: (
    input: DepositAsyncInput,
  ) => ReturnType<typeof getDepositInstructionAsync> & SelfPlanAndSendFunctions;
  initializeRegistry: (
    input: InitializeRegistryAsyncInput,
  ) => ReturnType<typeof getInitializeRegistryInstructionAsync> &
    SelfPlanAndSendFunctions;
  initializeStrategy: (
    input: InitializeStrategyAsyncInput,
  ) => ReturnType<typeof getInitializeStrategyInstructionAsync> &
    SelfPlanAndSendFunctions;
  rebalance: (
    input: RebalanceAsyncInput,
  ) => ReturnType<typeof getRebalanceInstructionAsync> &
    SelfPlanAndSendFunctions;
  setWeight: (
    input: SetWeightInput,
  ) => ReturnType<typeof getSetWeightInstruction> & SelfPlanAndSendFunctions;
  withdraw: (
    input: WithdrawAsyncInput,
  ) => ReturnType<typeof getWithdrawInstructionAsync> &
    SelfPlanAndSendFunctions;
};

export type VaultStrategyPluginPdas = {
  approvedAsset: typeof findApprovedAssetPda;
  registry: typeof findRegistryPda;
  shareMint: typeof findShareMintPda;
  strategy: typeof findStrategyPda;
};

export type VaultStrategyPluginRequirements = ClientWithRpc<
  GetAccountInfoApi & GetMultipleAccountsApi
> &
  ClientWithPayer &
  ClientWithTransactionPlanning &
  ClientWithTransactionSending;

export function vaultStrategyProgram() {
  return <T extends VaultStrategyPluginRequirements>(
    client: T,
  ): ExtendedClient<T, { vaultStrategy: VaultStrategyPlugin }> => {
    return extendClient(client, {
      vaultStrategy: <VaultStrategyPlugin>{
        accounts: {
          approvedAsset: addSelfFetchFunctions(client, getApprovedAssetCodec()),
          assetConfig: addSelfFetchFunctions(client, getAssetConfigCodec()),
          registry: addSelfFetchFunctions(client, getRegistryCodec()),
          strategy: addSelfFetchFunctions(client, getStrategyCodec()),
        },
        instructions: {
          addAsset: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getAddAssetInstructionAsync(input),
            ),
          approveAsset: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getApproveAssetInstructionAsync(input),
            ),
          collectFees: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getCollectFeesInstructionAsync({
                ...input,
                payer: input.payer ?? client.payer,
              }),
            ),
          deposit: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getDepositInstructionAsync(input),
            ),
          initializeRegistry: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getInitializeRegistryInstructionAsync(input),
            ),
          initializeStrategy: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getInitializeStrategyInstructionAsync(input),
            ),
          rebalance: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getRebalanceInstructionAsync(input),
            ),
          setWeight: (input) =>
            addSelfPlanAndSendFunctions(client, getSetWeightInstruction(input)),
          withdraw: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getWithdrawInstructionAsync(input),
            ),
        },
        pdas: {
          approvedAsset: findApprovedAssetPda,
          registry: findRegistryPda,
          shareMint: findShareMintPda,
          strategy: findStrategyPda,
        },
        identifyAccount: identifyVaultStrategyAccount,
        identifyInstruction: identifyVaultStrategyInstruction,
        parseInstruction: parseVaultStrategyInstruction,
      },
    });
  };
}

type MakeOptional<T, K extends keyof T> = Omit<T, K> & Partial<Pick<T, K>>;
