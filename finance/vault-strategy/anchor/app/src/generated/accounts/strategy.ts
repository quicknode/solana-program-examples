/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const STRATEGY_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  174, 110, 39, 119, 82, 106, 169, 102,
]);

export function getStrategyDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(STRATEGY_DISCRIMINATOR);
}

export type Strategy = {
  discriminator: ReadonlyUint8Array;
  /** Index used as the PDA seed, e.g. 0 for the first strategy. */
  index: bigint;
  manager: Address;
  /**
   * Registry whose curator approves assets. add_asset only accepts mints
   * approved in this registry.
   */
  registry: Address;
  shareMint: Address;
  usdcMint: Address;
  swapRouter: Address;
  /** Annual management fee in basis points (e.g. 100 = 1%). */
  feeBps: number;
  /**
   * Maximum tolerated deviation, in basis points, between a swap's output and
   * the Pyth-implied amount on deposit/rebalance. Bounded by MAX_SLIPPAGE_BPS.
   */
  maxSlippageBps: number;
  totalShares: bigint;
  lastFeeAccrualTimestamp: bigint;
  /**
   * Assets live at PDAs indexed 0..asset_count, so callers can re-derive the
   * complete set and no asset can be silently omitted from a NAV calculation.
   */
  assetCount: number;
  /** Running sum of every asset's target weight, kept <= 10000. */
  totalWeightBps: number;
  bump: number;
};

export type StrategyArgs = {
  /** Index used as the PDA seed, e.g. 0 for the first strategy. */
  index: number | bigint;
  manager: Address;
  /**
   * Registry whose curator approves assets. add_asset only accepts mints
   * approved in this registry.
   */
  registry: Address;
  shareMint: Address;
  usdcMint: Address;
  swapRouter: Address;
  /** Annual management fee in basis points (e.g. 100 = 1%). */
  feeBps: number;
  /**
   * Maximum tolerated deviation, in basis points, between a swap's output and
   * the Pyth-implied amount on deposit/rebalance. Bounded by MAX_SLIPPAGE_BPS.
   */
  maxSlippageBps: number;
  totalShares: number | bigint;
  lastFeeAccrualTimestamp: number | bigint;
  /**
   * Assets live at PDAs indexed 0..asset_count, so callers can re-derive the
   * complete set and no asset can be silently omitted from a NAV calculation.
   */
  assetCount: number;
  /** Running sum of every asset's target weight, kept <= 10000. */
  totalWeightBps: number;
  bump: number;
};

/** Gets the encoder for {@link StrategyArgs} account data. */
export function getStrategyEncoder(): FixedSizeEncoder<StrategyArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["index", getU64Encoder()],
      ["manager", getAddressEncoder()],
      ["registry", getAddressEncoder()],
      ["shareMint", getAddressEncoder()],
      ["usdcMint", getAddressEncoder()],
      ["swapRouter", getAddressEncoder()],
      ["feeBps", getU16Encoder()],
      ["maxSlippageBps", getU16Encoder()],
      ["totalShares", getU64Encoder()],
      ["lastFeeAccrualTimestamp", getI64Encoder()],
      ["assetCount", getU8Encoder()],
      ["totalWeightBps", getU16Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: STRATEGY_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Strategy} account data. */
export function getStrategyDecoder(): FixedSizeDecoder<Strategy> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["index", getU64Decoder()],
    ["manager", getAddressDecoder()],
    ["registry", getAddressDecoder()],
    ["shareMint", getAddressDecoder()],
    ["usdcMint", getAddressDecoder()],
    ["swapRouter", getAddressDecoder()],
    ["feeBps", getU16Decoder()],
    ["maxSlippageBps", getU16Decoder()],
    ["totalShares", getU64Decoder()],
    ["lastFeeAccrualTimestamp", getI64Decoder()],
    ["assetCount", getU8Decoder()],
    ["totalWeightBps", getU16Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link Strategy} account data. */
export function getStrategyCodec(): FixedSizeCodec<StrategyArgs, Strategy> {
  return combineCodec(getStrategyEncoder(), getStrategyDecoder());
}

export function decodeStrategy<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Strategy, TAddress>;
export function decodeStrategy<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Strategy, TAddress>;
export function decodeStrategy<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Strategy, TAddress> | MaybeAccount<Strategy, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getStrategyDecoder(),
  );
}

export async function fetchStrategy<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Strategy, TAddress>> {
  const maybeAccount = await fetchMaybeStrategy(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeStrategy<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Strategy, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeStrategy(maybeAccount);
}

export async function fetchAllStrategy(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Strategy>[]> {
  const maybeAccounts = await fetchAllMaybeStrategy(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeStrategy(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Strategy>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeStrategy(maybeAccount));
}

export function getStrategySize(): number {
  return 200;
}
