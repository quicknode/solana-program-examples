/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const APPROVED_ASSET_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  187, 133, 252, 71, 239, 199, 204, 13,
]);

export function getApprovedAssetDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    APPROVED_ASSET_DISCRIMINATOR,
  );
}

export type ApprovedAsset = {
  discriminator: ReadonlyUint8Array;
  registry: Address;
  mint: Address;
  priceFeed: Address;
  bump: number;
};

export type ApprovedAssetArgs = {
  registry: Address;
  mint: Address;
  priceFeed: Address;
  bump: number;
};

/** Gets the encoder for {@link ApprovedAssetArgs} account data. */
export function getApprovedAssetEncoder(): FixedSizeEncoder<ApprovedAssetArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["registry", getAddressEncoder()],
      ["mint", getAddressEncoder()],
      ["priceFeed", getAddressEncoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: APPROVED_ASSET_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link ApprovedAsset} account data. */
export function getApprovedAssetDecoder(): FixedSizeDecoder<ApprovedAsset> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["registry", getAddressDecoder()],
    ["mint", getAddressDecoder()],
    ["priceFeed", getAddressDecoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link ApprovedAsset} account data. */
export function getApprovedAssetCodec(): FixedSizeCodec<
  ApprovedAssetArgs,
  ApprovedAsset
> {
  return combineCodec(getApprovedAssetEncoder(), getApprovedAssetDecoder());
}

export function decodeApprovedAsset<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<ApprovedAsset, TAddress>;
export function decodeApprovedAsset<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<ApprovedAsset, TAddress>;
export function decodeApprovedAsset<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<ApprovedAsset, TAddress> | MaybeAccount<ApprovedAsset, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getApprovedAssetDecoder(),
  );
}

export async function fetchApprovedAsset<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<ApprovedAsset, TAddress>> {
  const maybeAccount = await fetchMaybeApprovedAsset(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeApprovedAsset<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<ApprovedAsset, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeApprovedAsset(maybeAccount);
}

export async function fetchAllApprovedAsset(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<ApprovedAsset>[]> {
  const maybeAccounts = await fetchAllMaybeApprovedAsset(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeApprovedAsset(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<ApprovedAsset>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeApprovedAsset(maybeAccount));
}

export function getApprovedAssetSize(): number {
  return 105;
}
