/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const ASSET_CONFIG_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  57, 112, 247, 166, 247, 64, 140, 23,
]);

export function getAssetConfigDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ASSET_CONFIG_DISCRIMINATOR,
  );
}

export type AssetConfig = {
  discriminator: ReadonlyUint8Array;
  strategy: Address;
  index: number;
  mint: Address;
  /**
   * Pyth PriceUpdateV2 account, copied from the registry's ApprovedAsset at
   * add time so the manager cannot substitute a feed they control.
   */
  priceFeed: Address;
  /** Strategy-owned associated token account holding this asset. */
  vault: Address;
  /**
   * Target share of the strategy's value in basis points. deposit deploys at these
   * weights (the sum across assets must reach 10000 before deposits open), and the
   * manager maintains them against price drift with rebalance.
   */
  weightBps: number;
  bump: number;
};

export type AssetConfigArgs = {
  strategy: Address;
  index: number;
  mint: Address;
  /**
   * Pyth PriceUpdateV2 account, copied from the registry's ApprovedAsset at
   * add time so the manager cannot substitute a feed they control.
   */
  priceFeed: Address;
  /** Strategy-owned associated token account holding this asset. */
  vault: Address;
  /**
   * Target share of the strategy's value in basis points. deposit deploys at these
   * weights (the sum across assets must reach 10000 before deposits open), and the
   * manager maintains them against price drift with rebalance.
   */
  weightBps: number;
  bump: number;
};

/** Gets the encoder for {@link AssetConfigArgs} account data. */
export function getAssetConfigEncoder(): FixedSizeEncoder<AssetConfigArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["strategy", getAddressEncoder()],
      ["index", getU8Encoder()],
      ["mint", getAddressEncoder()],
      ["priceFeed", getAddressEncoder()],
      ["vault", getAddressEncoder()],
      ["weightBps", getU16Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: ASSET_CONFIG_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link AssetConfig} account data. */
export function getAssetConfigDecoder(): FixedSizeDecoder<AssetConfig> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["strategy", getAddressDecoder()],
    ["index", getU8Decoder()],
    ["mint", getAddressDecoder()],
    ["priceFeed", getAddressDecoder()],
    ["vault", getAddressDecoder()],
    ["weightBps", getU16Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link AssetConfig} account data. */
export function getAssetConfigCodec(): FixedSizeCodec<
  AssetConfigArgs,
  AssetConfig
> {
  return combineCodec(getAssetConfigEncoder(), getAssetConfigDecoder());
}

export function decodeAssetConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<AssetConfig, TAddress>;
export function decodeAssetConfig<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<AssetConfig, TAddress>;
export function decodeAssetConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<AssetConfig, TAddress> | MaybeAccount<AssetConfig, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getAssetConfigDecoder(),
  );
}

export async function fetchAssetConfig<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<AssetConfig, TAddress>> {
  const maybeAccount = await fetchMaybeAssetConfig(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeAssetConfig<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<AssetConfig, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeAssetConfig(maybeAccount);
}

export async function fetchAllAssetConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<AssetConfig>[]> {
  const maybeAccounts = await fetchAllMaybeAssetConfig(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeAssetConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<AssetConfig>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeAssetConfig(maybeAccount));
}

export function getAssetConfigSize(): number {
  return 140;
}
