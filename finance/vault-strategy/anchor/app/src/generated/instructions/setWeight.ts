/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { VAULT_STRATEGY_PROGRAM_ADDRESS } from "../programs";

export const SET_WEIGHT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  41, 228, 200, 244, 87, 125, 173, 36,
]);

export function getSetWeightDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_WEIGHT_DISCRIMINATOR);
}

export type SetWeightInstruction<
  TProgram extends string = typeof VAULT_STRATEGY_PROGRAM_ADDRESS,
  TAccountManager extends string | AccountMeta<string> = string,
  TAccountStrategy extends string | AccountMeta<string> = string,
  TAccountAssetConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountManager extends string
        ? ReadonlySignerAccount<TAccountManager> &
            AccountSignerMeta<TAccountManager>
        : TAccountManager,
      TAccountStrategy extends string
        ? WritableAccount<TAccountStrategy>
        : TAccountStrategy,
      TAccountAssetConfig extends string
        ? WritableAccount<TAccountAssetConfig>
        : TAccountAssetConfig,
      ...TRemainingAccounts,
    ]
  >;

export type SetWeightInstructionData = {
  discriminator: ReadonlyUint8Array;
  weightBps: number;
};

export type SetWeightInstructionDataArgs = { weightBps: number };

export function getSetWeightInstructionDataEncoder(): FixedSizeEncoder<SetWeightInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["weightBps", getU16Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_WEIGHT_DISCRIMINATOR }),
  );
}

export function getSetWeightInstructionDataDecoder(): FixedSizeDecoder<SetWeightInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["weightBps", getU16Decoder()],
  ]);
}

export function getSetWeightInstructionDataCodec(): FixedSizeCodec<
  SetWeightInstructionDataArgs,
  SetWeightInstructionData
> {
  return combineCodec(
    getSetWeightInstructionDataEncoder(),
    getSetWeightInstructionDataDecoder(),
  );
}

export type SetWeightInput<
  TAccountManager extends string = string,
  TAccountStrategy extends string = string,
  TAccountAssetConfig extends string = string,
> = {
  manager: TransactionSigner<TAccountManager>;
  strategy: Address<TAccountStrategy>;
  assetConfig: Address<TAccountAssetConfig>;
  weightBps: SetWeightInstructionDataArgs["weightBps"];
};

export function getSetWeightInstruction<
  TAccountManager extends string,
  TAccountStrategy extends string,
  TAccountAssetConfig extends string,
  TProgramAddress extends Address = typeof VAULT_STRATEGY_PROGRAM_ADDRESS,
>(
  input: SetWeightInput<TAccountManager, TAccountStrategy, TAccountAssetConfig>,
  config?: { programAddress?: TProgramAddress },
): SetWeightInstruction<
  TProgramAddress,
  TAccountManager,
  TAccountStrategy,
  TAccountAssetConfig
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? VAULT_STRATEGY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    manager: { value: input.manager ?? null, isWritable: false },
    strategy: { value: input.strategy ?? null, isWritable: true },
    assetConfig: { value: input.assetConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("manager", accounts.manager),
      getAccountMeta("strategy", accounts.strategy),
      getAccountMeta("assetConfig", accounts.assetConfig),
    ],
    data: getSetWeightInstructionDataEncoder().encode(
      args as SetWeightInstructionDataArgs,
    ),
    programAddress,
  } as SetWeightInstruction<
    TProgramAddress,
    TAccountManager,
    TAccountStrategy,
    TAccountAssetConfig
  >);
}

export type ParsedSetWeightInstruction<
  TProgram extends string = typeof VAULT_STRATEGY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    manager: TAccountMetas[0];
    strategy: TAccountMetas[1];
    assetConfig: TAccountMetas[2];
  };
  data: SetWeightInstructionData;
};

export function parseSetWeightInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetWeightInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      manager: getNextAccount(),
      strategy: getNextAccount(),
      assetConfig: getNextAccount(),
    },
    data: getSetWeightInstructionDataDecoder().decode(instruction.data),
  };
}
