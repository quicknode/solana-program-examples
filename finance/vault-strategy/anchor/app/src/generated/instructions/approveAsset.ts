/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findApprovedAssetPda, findRegistryPda } from "../pdas";
import { VAULT_STRATEGY_PROGRAM_ADDRESS } from "../programs";

export const APPROVE_ASSET_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  127, 15, 21, 247, 23, 22, 189, 238,
]);

export function getApproveAssetDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    APPROVE_ASSET_DISCRIMINATOR,
  );
}

export type ApproveAssetInstruction<
  TProgram extends string = typeof VAULT_STRATEGY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountRegistry extends string | AccountMeta<string> = string,
  TAccountAssetMint extends string | AccountMeta<string> = string,
  TAccountApprovedAsset extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountRegistry extends string
        ? ReadonlyAccount<TAccountRegistry>
        : TAccountRegistry,
      TAccountAssetMint extends string
        ? ReadonlyAccount<TAccountAssetMint>
        : TAccountAssetMint,
      TAccountApprovedAsset extends string
        ? WritableAccount<TAccountApprovedAsset>
        : TAccountApprovedAsset,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ApproveAssetInstructionData = {
  discriminator: ReadonlyUint8Array;
  priceFeed: Address;
};

export type ApproveAssetInstructionDataArgs = { priceFeed: Address };

export function getApproveAssetInstructionDataEncoder(): FixedSizeEncoder<ApproveAssetInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["priceFeed", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: APPROVE_ASSET_DISCRIMINATOR }),
  );
}

export function getApproveAssetInstructionDataDecoder(): FixedSizeDecoder<ApproveAssetInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["priceFeed", getAddressDecoder()],
  ]);
}

export function getApproveAssetInstructionDataCodec(): FixedSizeCodec<
  ApproveAssetInstructionDataArgs,
  ApproveAssetInstructionData
> {
  return combineCodec(
    getApproveAssetInstructionDataEncoder(),
    getApproveAssetInstructionDataDecoder(),
  );
}

export type ApproveAssetAsyncInput<
  TAccountAuthority extends string = string,
  TAccountRegistry extends string = string,
  TAccountAssetMint extends string = string,
  TAccountApprovedAsset extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  registry?: Address<TAccountRegistry>;
  assetMint: Address<TAccountAssetMint>;
  approvedAsset?: Address<TAccountApprovedAsset>;
  systemProgram?: Address<TAccountSystemProgram>;
  priceFeed: ApproveAssetInstructionDataArgs["priceFeed"];
};

export async function getApproveAssetInstructionAsync<
  TAccountAuthority extends string,
  TAccountRegistry extends string,
  TAccountAssetMint extends string,
  TAccountApprovedAsset extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_STRATEGY_PROGRAM_ADDRESS,
>(
  input: ApproveAssetAsyncInput<
    TAccountAuthority,
    TAccountRegistry,
    TAccountAssetMint,
    TAccountApprovedAsset,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ApproveAssetInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountRegistry,
    TAccountAssetMint,
    TAccountApprovedAsset,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? VAULT_STRATEGY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    registry: { value: input.registry ?? null, isWritable: false },
    assetMint: { value: input.assetMint ?? null, isWritable: false },
    approvedAsset: { value: input.approvedAsset ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.registry.value) {
    accounts.registry.value = await findRegistryPda({
      authority: getAddressFromResolvedInstructionAccount(
        "authority",
        accounts.authority.value,
      ),
    });
  }
  if (!accounts.approvedAsset.value) {
    accounts.approvedAsset.value = await findApprovedAssetPda({
      registry: getAddressFromResolvedInstructionAccount(
        "registry",
        accounts.registry.value,
      ),
      assetMint: getAddressFromResolvedInstructionAccount(
        "assetMint",
        accounts.assetMint.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("registry", accounts.registry),
      getAccountMeta("assetMint", accounts.assetMint),
      getAccountMeta("approvedAsset", accounts.approvedAsset),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getApproveAssetInstructionDataEncoder().encode(
      args as ApproveAssetInstructionDataArgs,
    ),
    programAddress,
  } as ApproveAssetInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountRegistry,
    TAccountAssetMint,
    TAccountApprovedAsset,
    TAccountSystemProgram
  >);
}

export type ApproveAssetInput<
  TAccountAuthority extends string = string,
  TAccountRegistry extends string = string,
  TAccountAssetMint extends string = string,
  TAccountApprovedAsset extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  registry: Address<TAccountRegistry>;
  assetMint: Address<TAccountAssetMint>;
  approvedAsset: Address<TAccountApprovedAsset>;
  systemProgram?: Address<TAccountSystemProgram>;
  priceFeed: ApproveAssetInstructionDataArgs["priceFeed"];
};

export function getApproveAssetInstruction<
  TAccountAuthority extends string,
  TAccountRegistry extends string,
  TAccountAssetMint extends string,
  TAccountApprovedAsset extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_STRATEGY_PROGRAM_ADDRESS,
>(
  input: ApproveAssetInput<
    TAccountAuthority,
    TAccountRegistry,
    TAccountAssetMint,
    TAccountApprovedAsset,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ApproveAssetInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountRegistry,
  TAccountAssetMint,
  TAccountApprovedAsset,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? VAULT_STRATEGY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    registry: { value: input.registry ?? null, isWritable: false },
    assetMint: { value: input.assetMint ?? null, isWritable: false },
    approvedAsset: { value: input.approvedAsset ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("registry", accounts.registry),
      getAccountMeta("assetMint", accounts.assetMint),
      getAccountMeta("approvedAsset", accounts.approvedAsset),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getApproveAssetInstructionDataEncoder().encode(
      args as ApproveAssetInstructionDataArgs,
    ),
    programAddress,
  } as ApproveAssetInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountRegistry,
    TAccountAssetMint,
    TAccountApprovedAsset,
    TAccountSystemProgram
  >);
}

export type ParsedApproveAssetInstruction<
  TProgram extends string = typeof VAULT_STRATEGY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    registry: TAccountMetas[1];
    assetMint: TAccountMetas[2];
    approvedAsset: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: ApproveAssetInstructionData;
};

export function parseApproveAssetInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedApproveAssetInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      registry: getNextAccount(),
      assetMint: getNextAccount(),
      approvedAsset: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getApproveAssetInstructionDataDecoder().decode(instruction.data),
  };
}
