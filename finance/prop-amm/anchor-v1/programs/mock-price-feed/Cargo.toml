[package]
name = "mock_price_feed"
version = "0.1.0"
description = "Mock oracle price feed for testing the prop-amm program"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "mock_price_feed"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]
anchor-debug = []
custom-heap = []
custom-panic = []

[dependencies]
anchor-lang = "1.2.0"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(target_os, values("solana"))'] }
