{
  "type": "module",
  "scripts": {
    "build": "cargo build-sbf --manifest-path=./program/Cargo.toml --sbf-out-dir=./tests/fixtures",
    "build-and-test": "cargo build-sbf --manifest-path=./program/Cargo.toml --sbf-out-dir=./tests/fixtures",
    "test": "cargo test --manifest-path=./program/Cargo.toml",
    "deploy": "solana program deploy ./tests/fixtures/repository_layout_pinocchio_program.so"
  }
}
