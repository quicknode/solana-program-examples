//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const RETURN_CAR_DISCRIMINATOR: u8 = 3;

/// Accounts.
#[derive(Debug)]
pub struct ReturnCar {
    /// The account representing the active rental
    pub rental_account: solana_pubkey::Pubkey,
    /// The account representing the Car being rented in this order
    pub car_account: solana_pubkey::Pubkey,
    /// Fee payer
    pub payer: solana_pubkey::Pubkey,
}

impl ReturnCar {
    pub fn instruction(&self) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(&[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            self.rental_account,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.car_account,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.payer, true));
        accounts.extend_from_slice(remaining_accounts);
        let data = ReturnCarInstructionData::new().try_to_vec().unwrap();

        solana_instruction::Instruction {
            program_id: crate::CAR_RENTAL_SERVICE_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct ReturnCarInstructionData {
    discriminator: u8,
}

impl ReturnCarInstructionData {
    pub fn new() -> Self {
        Self { discriminator: 3 }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for ReturnCarInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

/// Instruction builder for `ReturnCar`.
///
/// ### Accounts:
///
///   0. `[writable]` rental_account
///   1. `[]` car_account
///   2. `[writable, signer]` payer
#[derive(Clone, Debug, Default)]
pub struct ReturnCarBuilder {
    rental_account: Option<solana_pubkey::Pubkey>,
    car_account: Option<solana_pubkey::Pubkey>,
    payer: Option<solana_pubkey::Pubkey>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl ReturnCarBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// The account representing the active rental
    #[inline(always)]
    pub fn rental_account(&mut self, rental_account: solana_pubkey::Pubkey) -> &mut Self {
        self.rental_account = Some(rental_account);
        self
    }
    /// The account representing the Car being rented in this order
    #[inline(always)]
    pub fn car_account(&mut self, car_account: solana_pubkey::Pubkey) -> &mut Self {
        self.car_account = Some(car_account);
        self
    }
    /// Fee payer
    #[inline(always)]
    pub fn payer(&mut self, payer: solana_pubkey::Pubkey) -> &mut Self {
        self.payer = Some(payer);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = ReturnCar {
            rental_account: self.rental_account.expect("rental_account is not set"),
            car_account: self.car_account.expect("car_account is not set"),
            payer: self.payer.expect("payer is not set"),
        };

        accounts.instruction_with_remaining_accounts(&self.__remaining_accounts)
    }
}

/// `return_car` CPI accounts.
pub struct ReturnCarCpiAccounts<'a, 'b> {
    /// The account representing the active rental
    pub rental_account: &'b solana_account_info::AccountInfo<'a>,
    /// The account representing the Car being rented in this order
    pub car_account: &'b solana_account_info::AccountInfo<'a>,
    /// Fee payer
    pub payer: &'b solana_account_info::AccountInfo<'a>,
}

/// `return_car` CPI instruction.
pub struct ReturnCarCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// The account representing the active rental
    pub rental_account: &'b solana_account_info::AccountInfo<'a>,
    /// The account representing the Car being rented in this order
    pub car_account: &'b solana_account_info::AccountInfo<'a>,
    /// Fee payer
    pub payer: &'b solana_account_info::AccountInfo<'a>,
}

impl<'a, 'b> ReturnCarCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: ReturnCarCpiAccounts<'a, 'b>,
    ) -> Self {
        Self {
            __program: program,
            rental_account: accounts.rental_account,
            car_account: accounts.car_account,
            payer: accounts.payer,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            *self.rental_account.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.car_account.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(*self.payer.key, true));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let data = ReturnCarInstructionData::new().try_to_vec().unwrap();

        let instruction = solana_instruction::Instruction {
            program_id: crate::CAR_RENTAL_SERVICE_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(4 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.rental_account.clone());
        account_infos.push(self.car_account.clone());
        account_infos.push(self.payer.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `ReturnCar` via CPI.
///
/// ### Accounts:
///
///   0. `[writable]` rental_account
///   1. `[]` car_account
///   2. `[writable, signer]` payer
#[derive(Clone, Debug)]
pub struct ReturnCarCpiBuilder<'a, 'b> {
    instruction: Box<ReturnCarCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> ReturnCarCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(ReturnCarCpiBuilderInstruction {
            __program: program,
            rental_account: None,
            car_account: None,
            payer: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// The account representing the active rental
    #[inline(always)]
    pub fn rental_account(
        &mut self,
        rental_account: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.rental_account = Some(rental_account);
        self
    }
    /// The account representing the Car being rented in this order
    #[inline(always)]
    pub fn car_account(
        &mut self,
        car_account: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.car_account = Some(car_account);
        self
    }
    /// Fee payer
    #[inline(always)]
    pub fn payer(&mut self, payer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.payer = Some(payer);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let instruction = ReturnCarCpi {
            __program: self.instruction.__program,

            rental_account: self
                .instruction
                .rental_account
                .expect("rental_account is not set"),

            car_account: self
                .instruction
                .car_account
                .expect("car_account is not set"),

            payer: self.instruction.payer.expect("payer is not set"),
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct ReturnCarCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    rental_account: Option<&'b solana_account_info::AccountInfo<'a>>,
    car_account: Option<&'b solana_account_info::AccountInfo<'a>>,
    payer: Option<&'b solana_account_info::AccountInfo<'a>>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
